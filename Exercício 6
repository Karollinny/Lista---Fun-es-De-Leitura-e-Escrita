def area(base, altura):
    return (base * altura) / 2

base = float(input("Digite a base do triângulo:"))
altura = float(input("Digite a altura do triângulo:"))

calculaArea = area(base, altura)

print(f"A área do triângulo é:", calculaArea)
